import { config } from "dotenv";
import { expand as dotenvExpand } from "dotenv-expand";
export function expand(envPaths, dotenvConfig) {
    envPaths.forEach((dotenvFile) => {
        dotenvExpand(config({
            path: dotenvFile,
            ...dotenvConfig,
        }));
    });
}
//# sourceMappingURL=expand.js.map