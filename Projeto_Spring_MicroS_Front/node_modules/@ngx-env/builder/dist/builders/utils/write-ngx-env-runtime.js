"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.writeRuntimeFile = writeRuntimeFile;
const fs_1 = require("fs");
function writeRuntimeFile(browserOutputDir, locales = [], raw) {
    // NODE_ENV should not be controlled via runtime
    delete raw.NODE_ENV;
    const runtimeStmt = `globalThis._NGX_ENV_ = ${JSON.stringify(raw, null, 2)};`;
    if (locales.length > 0) {
        locales.forEach((locale) => {
            console.log(`📦 Writing ngx-env.js to ${browserOutputDir}/${locale}/ngx-env.js`);
            try {
                (0, fs_1.writeFileSync)(`${browserOutputDir}/${locale}/ngx-env.js`, runtimeStmt);
            }
            catch (e) {
                console.log(`❌ Failed to create ngx-env.js at ${browserOutputDir}/${locale}/ngx-env.js ❌`);
                throw e;
            }
        });
    }
    else {
        try {
            console.log(`📦 Writing ngx-env.js to ${browserOutputDir}/ngx-env.js`);
            (0, fs_1.writeFileSync)(`${browserOutputDir}/ngx-env.js`, runtimeStmt);
        }
        catch (e) {
            console.log(`❌ Failed to create ngx-env.js at ${browserOutputDir}/ngx-env.js ❌`);
            throw e;
        }
    }
}
//# sourceMappingURL=write-ngx-env-runtime.js.map